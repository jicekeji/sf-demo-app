## 1. 项目脚手架与工具链

- [x] 1.1 初始化 TypeScript monorepo 结构（`frontend/` React + Vite + Tailwind，`backend/` Node HTTP API），配置 workspace 与共享 `tsconfig`
- [x] 1.2 接入测试运行器（后端 Vitest/Jest + supertest，前端 Vitest + React Testing Library），添加 `npm test` 脚本并确保空测试可运行
- [x] 1.3 配置 lint/format（ESLint + Prettier）与 CI 可用的 `npm run check` 脚本
- [x] 1.4 在前端接入设计令牌：将靛蓝主色 `#4f46e5`、间距（4/8/12/16/24/32）、圆角（sm 6 / md 10 / lg 16）、字号（xs–xl）与语义色写入 Tailwind config，验证令牌可被组件引用

## 2. 数据层与迁移（todo-persistence）

- [x] 2.1 先写失败测试：迁移脚本创建 `todos` 表（含 `id`、`user_id`、`title`、`completed`、`created_at`、`updated_at`），断言表结构存在
- [x] 2.2 实现首个迁移脚本使 2.1 通过（加列不删列原则，含 `user_id` 外键/索引）
- [x] 2.3 先写失败测试：同一迁移脚本重复执行两次不报错且 schema 不变（幂等 / 可重放）— 对应「迁移可重复执行」场景
- [x] 2.4 实现迁移的幂等保护（`IF NOT EXISTS` / 迁移版本记录表）使 2.3 通过
- [x] 2.5 先写失败测试：新增列后读取迁移前的旧记录，缺失新字段回落默认值仍可读 — 对应「旧记录保持可读」场景
- [x] 2.6 实现「加列带默认值」的向后兼容迁移使 2.5 通过
- [x] 2.7 实现数据访问层（repository）：按 `user_id` 隔离的 CRUD 查询，附带单元测试

## 3. 鉴权与数据归属（todo-persistence · fail-closed）

- [x] 3.1 先写失败测试：无有效会话的请求读/写待办返回未认证错误且不泄露任何内容 — 对应「未认证请求被拒」场景
- [x] 3.2 实现 fail-closed 鉴权中间件（缺省拒绝）使 3.1 通过
- [x] 3.3 先写失败测试：用户 A 请求列表仅返回 A 的事项，不含其他用户数据 — 对应「只见本人数据」场景
- [x] 3.4 在 repository/handler 层强制 `user_id` 过滤使 3.3 通过

## 4. 后端 API：待办管理（todo-management）

- [x] 4.1 先写失败测试：`POST /todos` 用非空标题「买牛奶」创建，返回默认未完成状态并入库 — 对应「使用非空标题创建」场景
- [x] 4.2 实现创建接口使 4.1 通过
- [x] 4.3 先写失败测试：空标题或纯空白标题被拒绝、不新增事项并返回「标题不能为空」— 对应「拒绝空标题」场景
- [x] 4.4 先写失败测试：标题超过 200 字符被拒绝、不新增事项并返回「标题过长」— 对应「标题超长截断校验」场景
- [x] 4.5 实现创建时的标题校验（非空 + ≤200）使 4.3、4.4 通过
- [x] 4.6 先写失败测试：`GET /todos` 稳定顺序返回全部事项的标题与完成状态 — 对应「列表包含已有事项」场景
- [x] 4.7 先写失败测试：无事项时返回空集合（供前端渲染空状态）— 对应「空列表提示」场景
- [x] 4.8 实现列表接口（稳定排序）使 4.6、4.7 通过
- [x] 4.9 先写失败测试：`PATCH /todos/:id` 修改标题成功持久化新标题 — 对应「成功修改标题」场景
- [x] 4.10 先写失败测试：编辑时清空标题被拒绝、保留原标题并提示不能为空（复用创建校验）— 对应「编辑时拒绝空标题」场景
- [x] 4.11 实现编辑标题接口（复用校验规则）使 4.9、4.10 通过
- [x] 4.12 先写失败测试：`DELETE /todos/:id` 删除已有事项后列表不再包含它、其余顺序不变 — 对应「删除已有事项」场景
- [x] 4.13 先写失败测试：删除不存在/已删除的 id 不改变数据并返回明确「未找到」— 对应「删除不存在的事项」场景
- [x] 4.14 实现删除接口使 4.12、4.13 通过

## 5. 后端 API：完成状态（todo-completion）

- [x] 5.1 先写失败测试：将未完成事项标记为已完成后状态更新为已完成 — 对应「标记为已完成」场景
- [x] 5.2 先写失败测试：将已完成事项标记为未完成后状态恢复为未完成 — 对应「取消完成」场景
- [x] 5.3 实现 `PATCH /todos/:id` 完成状态切换使 5.1、5.2 通过
- [x] 5.4 先写失败测试：`GET /todos?status=active` 仅返回未完成事项 — 对应「只看未完成」场景
- [x] 5.5 先写失败测试：`GET /todos?status=completed` 仅返回已完成事项 — 对应「只看已完成」场景
- [x] 5.6 先写失败测试：`GET /todos?status=all`（或缺省）返回全部事项 — 对应「查看全部」场景
- [x] 5.7 实现按状态筛选查询使 5.4、5.5、5.6 通过
- [x] 5.8 先写失败测试：列表响应包含未完成计数字段，标记完成后计数减一 — 对应「计数随状态变化更新」场景
- [x] 5.9 实现未完成计数（响应字段或独立字段）使 5.8 通过

## 6. 前端：设计系统组件与页面骨架

- [x] 6.1 先写失败测试：渲染 Button（primary/ghost/danger）、Input、Card/List 基础组件，断言使用设计令牌类且键盘可聚焦（可访问性）
- [x] 6.2 实现上述基础组件使 6.1 通过，复用第 1.4 步的令牌
- [x] 6.3 实现 API 客户端封装（fetch + 会话凭证 + 错误归一化），附带 mock 测试
- [x] 6.4 搭建 TodoApp 页面骨架（列表区 + 输入区 + 筛选区 + 计数区）并接入路由外的单页结构

## 7. 前端：待办管理交互（todo-management）

- [x] 7.1 先写失败测试：输入非空标题提交后列表出现新的未完成事项 — 对应「使用非空标题创建」场景
- [x] 7.2 先写失败测试：提交空/空白标题时不新增事项并显示「标题不能为空」错误 — 对应「拒绝空标题」场景
- [x] 7.3 先写失败测试：标题超过 200 字符时显示「标题过长」且不提交
- [x] 7.4 实现创建表单与前端校验使 7.1、7.2、7.3 通过
- [x] 7.5 先写失败测试：无事项时展示空状态提示与引导 — 对应「空列表提示」场景
- [x] 7.6 实现列表与空状态渲染使 7.5 通过
- [x] 7.7 先写失败测试：编辑事项标题保存后列表展示新标题；清空标题保存时保留原标题并提示 — 对应「成功修改标题」「编辑时拒绝空标题」场景
- [x] 7.8 实现内联编辑交互使 7.7 通过
- [x] 7.9 先写失败测试：删除事项后其从列表消失、其余顺序不变 — 对应「删除已有事项」场景
- [x] 7.10 实现删除交互使 7.9 通过

## 8. 前端：完成状态与筛选（todo-completion）

- [x] 8.1 先写失败测试：勾选未完成事项后呈现已完成视觉（删除线/勾选），再次点击恢复未完成 — 对应「标记为已完成」「取消完成」场景
- [x] 8.2 实现完成状态切换与视觉标记使 8.1 通过
- [x] 8.3 先写失败测试：选择「未完成」「已完成」「全部」筛选分别只显示对应事项 — 对应三个筛选场景
- [x] 8.4 实现筛选标签页/切换使 8.3 通过
- [x] 8.5 先写失败测试：标记完成后未完成计数即时减一 — 对应「计数随状态变化更新」场景
- [x] 8.6 实现未完成计数展示使 8.5 通过

## 9. 前端：持久化与错误处理（todo-persistence）

- [x] 9.1 先写失败测试：刷新/重开后重新加载并展示此前保存的全部事项及完成状态 — 对应「刷新后数据保留」场景
- [x] 9.2 实现初始化加载（从后端拉取）使 9.1 通过
- [x] 9.3 先写失败测试：保存因后端不可用失败时提示「保存失败」并保留用户已提交内容以便重试、不静默丢弃 — 对应「写入失败时不丢已存数据」场景
- [x] 9.4 实现失败提示与内容保留/重试机制使 9.3 通过

## 10. 可观测性与非功能基线

- [x] 10.1 先写失败测试：`GET /health` 返回健康探测响应
- [x] 10.2 实现健康探测端点使 10.1 通过
- [x] 10.3 实现结构化日志（请求/错误 JSON 日志，含请求标识，不记录敏感数据），添加最小断言测试
- [x] 10.4 校验错误响应统一格式（未认证、校验失败、未找到），补充覆盖测试

## 11. 验收与收尾

- [x] 11.1 运行前后端全部测试与 lint，确保 TDD 用例全部通过、覆盖三个 capability 的全部 Scenario
- [x] 11.2 端到端手工验证：创建 → 编辑 → 完成/取消 → 筛选 → 计数 → 删除 → 刷新保留
- [x] 11.3 复核不改动现有 `index.html` 静态骨架，确认未引入路由/多用户共享/提醒推送等超范围特性
- [x] 11.4 运行 `openspec validate todo-f8c546dc --strict` 通过后归档准备
